# Weilai-01 灵活性修补册

> 范围：审计「路径/值/函数被写死、极依赖本机或账号环境」的硬编码点，登记并分批修补。
> 基线：源码现状（与 `docs/工程总报告.md` §8 技术债对齐）。本册=硬编码灵活性维度的活清单，原地更新。
> 日期：2026-06-27 ｜ 已改：**A4 + M3**（前轮）→ **A7 + M2 + A9 + 魔数(M7/M8/M10/T1/T2) + P1 + A8**（本轮·批 A 项1~4 + 批 B 项5~6，全部行为字节等价）。**剩 P2/P3 + N 通道泛化**（明确暂缓，见 §3/§4）。

## 0. 定性前提（先分清"写死"与"已外提"）

`system.json` + `channels/*.json` 已把**大部分**硬编码外提为配置（换机/换号改 JSON 即可，设计良好）。真正"需要灵活却仍被写死"的只有三类：

- 🔴 **源码写死**：值/路径/函数硬编在 `.mjs`，换环境须改代码。
- 🟠 **双源/魔数**：config 与源码各存一份、可能（已）漂移。
- ⚪ **疑 dead**：配置项源码未引用。

图例下同。🟡=已正确外提到配置，仅作存档、非"待改"。

---

## 1. 修补登记表（状态总账）

| 重点 | ID | 写死内容 | 位置 | 性质 | 状态 |
|---|---|---|---|---|---|
| ① | **A4** | 通道事实双源 + `jie3.maxUploads` 漂移(5 vs 7) | `state.mjs:8` ↔ `channels/jie3.json:12` | 🟠 | ✅ 前轮已改 |
| ④ | **M3** | 视口 `1920×1080` 双写 | `system.json:17` ↔ `cdp.mjs:108` | 🟠 | ✅ 前轮已改 |
| ② | **P1** | 平台 URL/域名写死 | `guard.mjs`、`session.mjs`、`telemetry.mjs` | 🔴 | ✅ **本轮已改**（→ `selectors.mjs`） |
| ② | P2 | DOM 选择器/class 写死（~130 处） | session/upload/delete | 🔴 | ⬜ 暂缓（平台改版触发） |
| ② | P3 | 中文按钮/文案判据写死 | session/submit/upload | 🔴 | ⬜ 暂缓（同 P2） |
| ③ | **A8** | 双通道名 `['jie3','jie6']` 硬假设 | config/cmds/clearlocal/run | 🔴 | ✅ **本轮已改**（命令层·保双通道不变量） |
| ③ | **A9** | 通道特例分支 `id==='jieX'` | `flywheel.mjs:62,95` | 🔴 | ✅ **本轮已改**（读 role/ui） |
| — | **A7** | `sso.handshakeAdvId` 源码未引用 | `system.json:32` | ⚪ | ✅ **本轮已删**（证实 dead） |
| — | **M2** | profile 派生规则 `id==='jie3'?'':'-'+id` | `config.mjs` | 🔴 | ✅ **本轮已改**（`profileSuffix` 字段） |
| — | **M7** | CDP 端口 fallback `9222` | `cdp.mjs` | 🔴 | ✅ **本轮已改**（→ `selectors.CDP_PORT`） |
| — | **M8** | CDP 主机 `127.0.0.1` | cdp/session | 🔴 | ✅ **本轮已改**（→ `selectors.CDP_HOST`） |
| — | **M10** | 批大小 fallback `9` vs config `10` | `upload.mjs:64` | 🟠 | ✅ **本轮已改**（对齐 10） |
| — | **T1/T2** | 魔数 30min/grace30 | `upload.mjs`、`reconcile.mjs` | 🟠 | ✅ **本轮已改**（→ `timeouts`） |

> 已正确外提、不在待改之列（🟡）：Chrome/ffmpeg/md5fix/台账路径、账户名/aavid/planId、关键词 `魏文彬`、母账号凭据、`timeouts/concurrency/batch/daemon`。详见线框表一~四（对话记录）。

---

## 2. 本轮变更明细（changelog）

### ✅ 重点①(A4)：通道事实归一到 `channels/*.json`，消除 maxUploads 漂移

**现象**：`lib/state.mjs::DEFAULT_CHANNELS` 把 `account/aavid/planId/maxUploads` 等通道事实又硬编一份，与 `channels/*.json` 重复；且 `jie3.maxUploads` 已漂移——`state.mjs`=**5**，`channels/jie3.json` 与线上台账=**7**。

**为何潜伏未爆**：`ledger.mjs::applySync` 与 `sync.mjs` 在**每次提交/读取**时 `loadChannels()(读 channels) → syncChannels` 覆盖台账 channels，磁盘台账 `_video_state.json` 已是 `maxUploads:7`。故 `DEFAULT_CHANNELS=5` 仅在两处生效：① 台账文件**不存在**（换机引导）；② 离线 `test/reconcile-test.mjs`（直接 import `DEFAULT_CHANNELS`）。属**潜伏地雷 + 灵活性债**（改账户要动两文件），非当前 live bug。

**修法**：让 `loadState` 的引导 channels/pipeline 经新增 `bootChannels()` 从 `config.loadChannels()`（即 `channels/*.json`）取，读不到（离线/测试缺 channels）才回退 `DEFAULT_*`；并把 `DEFAULT_CHANNELS.jie3.maxUploads` 5→7 使离线兜底亦正确。**`channels/*.json` 自此为通道事实唯一可编辑真源**（改账户/计划/maxUploads 只改 channels）。

**文件**：`lib/state.mjs`（+`import {loadChannels}`；+`bootChannels()`；`loadState` 用之；`5→7`；注释强化）。

**安全性**：`state→config` 单向依赖（config 仅依赖 node 内建，无环）；`bootChannels` 全程 try/catch 回退，不新增抛错路径。对现有台账行为零变化（本就被 channels 覆盖为 7）；仅"换机引导/离线测试"现取真源。

**验证**：`node test/reconcile-test.mjs`、`node test/ledger-concurrency.mjs` 通过（见 §3）。

> 备注：`docs/工程总报告.md` §3.3 状态机图仍写"jie3 传满 **5** 次"，为陈旧文案（真值 7），属文档债、另行修订；本轮不动报告正文。

### ✅ 重点④(M3)：视口以 `--window-size` flag 为单一真源

**现象**：`system.json` 的 `--window-size=1920,1080`（定真实窗口）与 `cdp.mjs::hardenViewport` 的 `setDeviceMetricsOverride({width:1920,height:1080})`（定虚拟视口）各写一份。改窗口尺寸会漏改源码 → 虚拟视口≠真实窗口 → `getBoundingClientRect` 坐标点空（即 NO_CHOOSER 同根因）。

**修法**：以 `--window-size` flag 为唯一真源——`config.loadSystem()` 新增 `parseWindowSize()` 解析该 flag，派生 `system.chrome.viewport={width,height}`（flag 缺失兜底 1920×1080）；`hardenViewport(cdp, viewport)` 改为接收视口（默认仍 1920×1080，护住测试/零散调用）；5 处调用传 `system.chrome.viewport`。

**文件**：`lib/config.mjs`（+`parseWindowSize`；`loadSystem` 派生 viewport）、`lib/cdp.mjs`（`hardenViewport` 签名）、`lib/upload.mjs:166`、`lib/delete.mjs:126,144`、`lib/session.mjs:211,261`（传参）。

**安全性**：行为完全保持——当前 flag=`1920,1080` ⟹ viewport=1920×1080，与原硬编逐字节相同。今后只改 flag 即同时驱动真实窗口与虚拟视口，二者不再可能分叉。

**验证**：模块加载自检（`node -e` 载入 config/cdp）+ `parseWindowSize` 单测式核对；坐标端到端需真跑 upload，但派生值=原值，等价。

### ✅ 本轮（批 A 项1~4 + 批 B 项5~6）：架构脚手架 + 8 项硬编码外提

> 全部**行为字节等价**（profile / 5 条 URL builder / 5 条正则逐字符比对原值），与 A4/M3 同风格；4 个离线测试每批后均绿。

**新增架构（单一真源）**
- `lib/selectors.mjs` —— 平台绑定单一真源：CDP 端点(host/port) + 平台导航 URL builder + URL 判据正则。平台一改只改此一文件（P2/P3 选择器/文案将来作新 section 落）。
- `lib/config-write.mjs` —— 配置原子写（镜像 `state.saveState`：tmp→.bak→rename）+ 点分键读写 + 标量类型转换。
- `lib/config.mjs` —— 加 `discoverChannelIds()` / `channelRegistry()` / `assertChannel()`；导出 `REQUIRED_TARGET`。

**新命令 `weilai config get/set`**（命令可改运营旋钮，根治 06-26 手改 `maxUploads→999` 求裁决的痛点）
- `get <system|jie3|jie6> <a.b.c>`；`set ... <value>` **默认 dry-run**，`--apply` 才落盘(+.bak)。
- 只命令化 **ASCII 字段**（maxUploads/planId/aavid/port/role/ui/funded）；中文值(kw/account/mode)被 argv ASCII 铁律拦下→提示手改 JSON（不削弱铁律）。
- 敏感字段护栏：aavid/planId 警"指向错账户/计划"；funded 号上任何敏感字段警"真实支出"。

**8 项外提**
- **A7**：删 `system.json.sso`（grep 证实全源码无引用，握手走 `target.advId`）。
- **M2**：`channels/*.json` 加 `profileSuffix`，`config.loadConfig` 改读它（缺字段回退旧规则·零破坏）。
- **A9**：`flywheel` 的 `id==='jie6'/'jie3'` → 读 `target.role` / `target.ui`。
- **M7/M8**：CDP host/port → `selectors.CDP_HOST/PORT`（真实端口仍来自 `target.port`，仅默认兜底集中）。
- **M10**：`upload.mjs` 批大小 fallback `9`→`10`（对齐 config，消漂移）。
- **T1/T2**：`upload` rebump 窗 / `reconcile` grace → `system.json.timeouts.{rebumpWindowMin,reconcileGraceMin}`。
- **P1**：~10 导航 URL + 5 判据正则 → `selectors.mjs`（页内注入的 looksLogin 跑浏览器侧、引用不到 Node 常量，按设计留原处）。
- **A8**：命令层 18 文件去 `jie3/jie6` 字面量，改读 `channelRegistry` / `target.role`；**双通道不变量("恰好 1 test + 1 delivery")集中成 `channelRegistry()` 唯一断言**——多丢一个 channels/*.json 即 `E_CONFIG`。**`state.mjs` 2 段状态机不动**（已按 `pipeline`/`role` 泛化，换名即安全）。

**验证**：4 离线测试全绿；profile / 5 URL builder / 5 正则逐字符比对原值；config 命令 get/set/dry-run/apply/.bak/敏感⚠/中文拦 全过；`assertChannel` 拒未知 id(exit 2)、合成第 3 通道触发不变量断言(E_CONFIG)。

---

## 3. 待改 backlog（剩余·均暂缓）

- **②(P2/P3) DOM 选择器 + 中文文案**（🔴，工程总报告 §8 头号技术债）：~130 处选择器 + 按钮文案写死，平台改版即 `E_SELECTOR` 停摆。**明确暂缓**——高风险、价值虚（止痛不治本，报告 §8.2）、且须在有钱号上才能验。集中方案同 P1（落 `selectors.mjs` 新 section），等平台真改版触发再做。
- **N 通道泛化**（🔴，架构）：当前 A8 只做"改名/换号安全"，保留"恰好 2 通道(1 test + 1 delivery)"不变量并显式断言。要支持第 3 通道需重写 `state.recomputeStage`/`worklists` 的 2 段流水线语义——工程量大、动台账核心、无业务需求，**暂缓**。

---

## 4. 决策建议

A4/M3/本轮 8 项均为**零风险止血/外提**（行为字节等价）。剩 **P2/P3 选择器+文案** 与 **N 通道泛化** 已明确暂缓——前者触及"平台脆弱性"本质（堆工程量也填不平，报告 §8.2），后者无业务需求。两者均**由投放业务量触发**：仅 jie3 日常用 → 维持现状即可；要规模化 jie6 → 再排 P2/P3(选择器集中) + 工程总报告 §6 的 jie6 delete/登录链。
