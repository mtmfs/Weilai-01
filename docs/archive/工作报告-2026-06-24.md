# Weilai-01 CLI 工作报告

**日期**：2026-06-24
**仓库**：https://github.com/mtmfs/Weilai-01 （private）
**最新提交**：`df858fd`（main，已 push origin）
**操作者**：业务主管（不写代码） ｜ **维护**：AI
**接续**：本报告承接 `工作报告-2026-06-23.md`（停在 `3c2e533`·计划①交付）；当日又叠了一整段从计划①验收到计划②上传 + 性能路线图 Phase 1–4 的推进，以本报告为当前现状。

---

## 一、一句话摘要

把上传从「完全没实现的桩」推进到「真能在 jie3 上传出素材、记账、测过审率」的数据闭环——**计划②核心 live 验证通过**；并完成性能路线图 **Phase 1（提速）/ S1（增量同步）/ S5+pass-rate 基建**。

**当前可用边界**：jie3（测试通道）的非上传 + 上传操作，在 profile 保持登录、磁盘有空间的前提下能用——覆盖「海投筛过审」整段。

---

## 二、本阶段产出（2026-06-24，main 推进 11 个提交）

### 性能/正确性（perf-roadmap → 合 main）
| 提交 | 内容 | 验证 |
|---|---|---|
| `85d09a1` | A 系列 10 bug 修复 | 离线 12 单测 + live |
| `d2aec32` | C2/C3/C4 提速（sync 19s→10.9s） | ✅ live |
| `4dad860` | C1/C5/S8 ready 收敛提速 | ✅ live（见三） |
| `13ad129` | S5 Thompson bandit 模块 | 离线 7/7 |
| `4239dd8` | Phase 1 提速 live 验证 | ✅ |

### S1 mid 增量同步（Phase 2）
| 提交 | 内容 | 验证 |
|---|---|---|
| `db7a429` | S1a：`pullRowsByMid`+`runSyncIncremental`+`--incremental` opt-in | ✅ live 对账 13/13 |
| `e4c3d91` | S1b：op 签名缓存（跳 reload）+ 失效自动回退 | ✅ live（缓存命中/回退自愈） |

### 计划②上传核心（Phase 3）
| 提交 | 内容 | 验证 |
|---|---|---|
| `d86ca2c` | inject 瞬态注入 + submit 逐文件超时 + bump + upload 命令 + cycle 集成 | ✅ **live 真创建素材** |
| `efa2043` | 硬化：轮次 token 幂等 + openView 冷启重试 + test-round/deliver-round | ✅ live（openView 重试自愈）+ dry |

### Phase 4 数据驱动（pass-rate 基建）
| 提交 | 内容 | 验证 |
|---|---|---|
| `df858fd` | pass-rate 基建（submissions.jsonl）+ **point7 mid 捕获** + S5 bandit 接线 + `passrate` 命令 | 离线 11/11 + mid 捕获 read-only live |

**命令面**：`status / ready / close / sync(--incremental) / delete / md5fix / prep / upload / test-round / deliver-round / cycle / monitor / stats / passrate`。

---

## 三、验证状态（诚实版）

**✅ live 验证（真账户 jie3）**
- **Phase 1 提速**：冷启 **32.1s**（基线≈39s）/ 热路径 **0.9s**（S8）/ 仅抽屉关 **1.4s**（C5）。真实赢点＝温/热/抽屉关路径，操作者日常 re-ready ~40s→~1s。
- **S1a 对账**：13 在飞件 `{last_status,passed,scrapped,stage}` 与全量逐字段一致；查 **0.3s vs 3.4s**；台账零改动。
- **S1b 签名缓存**：实测 op 签名 TTL **≥10min** → budget 7min；cache 命中 **6s→1s**；坏签名→E_SIG→回退现抓自愈。
- **计划②上传**：jie3 真上传 113MB md5fix'd 文件 → 素材创建成功（id=`…946534`），「确认添加并开始投放 1 个素材」提交确认。
- **openView 冷启重试**：冷启「视图未现→重试2/3」自愈（根治本会话两次 E_SELECTOR）。
- **point7 mid 捕获**：max-mid 选择逻辑 read-only 确认 `测试_12 → …946534`。

**🟡 离线/dry 验证**
- bump 轮次 token 幂等（逻辑 5/5）；pass-rate 全管线（record/resolve/幂等/stats/bandit arms 11/11）。

**⬜ 未 live 跑（部件已分别验证）**
- 完整 `upload`/`test-round` 命令 e2e（worklist→md5fix→上传→bump→sync 结审）——被磁盘卡。
- `deliver-round`/jie6 整条；`login()`（jie3 一直登录态从没跑）；`monitor` record()。

**真实数据点**：本会话上传的 `测试_12`（`…946534`）已被平台**审核拒绝**（audit 3→2），是一个真实的「已结提交」，下次磁盘清后能让 pass-rate 管线吃到真数据。

---

## 四、当前阻塞 & 已知问题

1. **★ C 盘满（头号阻塞）**：会话结束时 C: 0 可用。**任何起 Chrome 的 live 验证/真上传都做不了**，清出几 G 才能继续。
   - 排查结论：我的**直接** C: 写入仅 56K（scratchpad 脚本）；调试 Chrome 的 profile/缓存落在 **I:\chrome-debug-profile（886M）**，不在 C:。C: 满主因＝**用户常规 Chrome Default(742M)+Edge/Steam/Windows 系统缓存**（会话数小时内自身活动）。无法完全排除我反复冷启 Chrome 时往 C: 写过已清理的临时数据。**教训：盘紧时应更克制起 Chrome。**
   - 铁律不变：绝不 `taskkill chrome`（触发 Crashpad 反噬 C:），只用 `weilai close`。
2. **jie6 冷 profile 登录未通**：挡住整条投放通道（jie6 ready/创意tab上传/delete/sweep/双实例）。根因＝登录探测链（#2/#3/#5）。jie6=有钱账户（投放中~49万），谨慎。
3. **计划②限制**：hold-submit 留桩（需延迟挂起 TTL 探针）；逐文件超时现用 DOM %-stall（Network 事件驱动＝后续精化）；轮次 token 幂等是 30min 时间窗护栏（非严格幂等）。
4. **cycle 是单趟**：多轮收敛循环（丛3·轮间人控点）未实现。
5. **副产物**：`测试_12` 素材留平台待清（现 audit=2，可 delete）。

---

## 五、剩余工作（工作量+难度，按阻塞分组）

> 真正卡死的只有两件：① C 盘（挡 live）② jie6 登录（挡投放通道）。清掉这俩，大半能推进。

### A. 收尾验证（难度低，缺"跑一次"，卡 C 盘）
| 项 | 改动量 | 难度 | 阻塞 |
|---|---|---|---|
| 完整 `test-round jie3` e2e（验②标准） | 0 | 低 | 清 C 盘 |
| 多轮 cycle 收敛循环（loop + 轮间人控点） | 中 | 中 | 无（jie3 可做） |
| `monitor` record() live 验 | 0 | 低 | 清 C 盘 |

### B. jie6 投放通道（难度中-高，全卡 jie6 登录）
| 项 | 改动量 | 难度 | 阻塞 |
|---|---|---|---|
| **登录探测链修复（#2/#3/#5·一箭三雕）** | 中 | 中-高 | 需在登出 profile 调试 `login()`（有风险） |
| jie6 创意tab 上传收敛（推直播间 UI≠drawer） | 中 | 高 | jie6 登录 + 谨慎 |
| jie6 delete / sweep（预抓 set-opt 重放） | 小-中 | 中 | jie6 ready |
| 双实例 both 真测（#5/S4） | 小-中 | 中 | jie6 登录 |

### C. 上传深化（难度中-大，无硬阻塞，纯 jie3 增量）
| 项 | 改动量 | 难度 | 价值 |
|---|---|---|---|
| 延迟挂起 TTL 探针（R&D） | 小-中 | 中 | 决定 hold-submit 能否转正 |
| hold-submit 转正 + 接 bandit 择时 | 中 | 中-高 | 锦上添花（即时提交已解拖累） |
| 批间流水线（上批传时注入下批） | 中 | 中 | 提速 |
| S3 精化：Network 事件驱动完成信号 | 中 | 中 | 更精确逐文件 |
| **S2 连续流水线（常驻调度·Little定律）** | 大 | 高 | 吞吐天花板（S1+S3 已具备） |

### D. 数据驱动（难度中，卡"积累真实数据"）
| 项 | 改动量 | 难度 | 阻塞 |
|---|---|---|---|
| 积累真实 pass-rate（跑几轮真上传） | 0 | 低 | 清盘+时间 |
| S7 重试预算按 pass_rate | 小 | 中 | 需上面数据 |
| S6 ffprobe 上传前预校验 | 小-中 | 中 | 需平台必拒阈值（领域数据） |

### E. 长期 / 加固（可选）
| 项 | 改动量 | 难度 | 备注 |
|---|---|---|---|
| 选择器集中 + `selftest` | 小-中 | 低 | 破坏性运行前验漂移 |
| S9 API-direct 上传（逆向 VOD 签名） | 大 | 极高 | 可能引 Go，长期 R&D |

---

## 六、台账现状（只读快照）

视频总数 **97**：testing=13 / sealed=4 / delivering=10 / delivered=15 / scrapped=55。
flat 根目录 `H:\DD\6-18-魏-指纹\`（82 个本地魏文彬 mp4）。

---

## 七、下一步（按性价比）

1. **清 C 盘**（只清你自己的浏览器/系统缓存，**勿动 `I:\chrome-debug-profile`**＝jie3 登录态）。
2. 清盘后 **`test-round jie3` 跑一轮真上传** → 验②标准「已传完不陪等5min」+ 顺带喂活 pass-rate / S1 live-audit。
3. 想解锁投放通道：啃**登录探测链**（一箭三雕，解 jie3 鲁棒性 + jie6 第一道坎）。
4. 想提吞吐：上传深化 C 组（无阻塞，纯 jie3）→ 最终 S2 连续流水线。

---

## 附：本阶段提交（2026-06-24）

```
df858fd Phase4 pass-rate 基建 + point7 mid 捕获 + S5 接线
efa2043 Phase3 上传硬化（轮次token幂等 + openView重试 + test/deliver-round）
d86ca2c Phase3/S3 上传核心（inject + submit逐文件超时 + bump + upload命令）
e4c3d91 S1b op 签名缓存（跳 reload + 失效回退）
db7a429 S1a mid 增量同步（pullRowsByMid + runSyncIncremental + --incremental）
4239dd8 Phase 1（C1/C5/S8）live 验证 + 满盘阻塞解除
622233f 优化 backlog（C/S 系列状态·路线图）
13ad129 S5 Thompson 采样 bandit 模块
4dad860 ready 收敛提速 C1/C5/S8 + A8 spawn 守护
d2aec32 C2/C3/C4（sync 4并发 + 捕签名 early-return + md5 流式哈希）
85d09a1 A 系列 bug 修复（10 个）
```
（更早见 `工作报告-2026-06-23.md`：计划①交付、混沌测试、Phase 0 骨架。）
</content>
