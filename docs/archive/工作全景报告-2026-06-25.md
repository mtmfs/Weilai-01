# Weilai-01 工作全景报告（三源交叉验证）

> 生成：2026-06-25 ｜ 方法：**源码 ⨯ git ⨯ 文档（工作报告/buglog/PLAN）三源交叉验证**。
> 验证手段：通读 `bin/`+`lib/` 全部源码、`git diff`(工作区) / `git log`(提交史) 逐文件对账、`node --check` 工作区 9 文件语法核验、只读 `status` 取台账实况。
> 本报告回答的核心问题：**哪些改动已 commit 落盘、哪些只在工作区未落盘、哪些落盘过又被改。**

---

## 0. 一句话现状

代码主线已推进到 **计划②上传核心 live 验证通过 + 双通道闭环跑通**（jie3 筛过审 → jie6 真投放，14 件真投放中）；性能路线图 **Phase 1–4 全部接线**。但 **2026-06-25 第二段实跑产出的 9 处改动（fail-loud / kw 字段锁 / cycle 多轮收敛 / sync↔delete 共享快照）全部留在工作区未提交**，用户喊停在「写报告、代码先不改」。HEAD 仍是 `70fb006`，工作区干净度 = 9 改 + 2 未跟踪报告。

---

## 1. ★ 落盘真相表（本报告的核心交付）

### 1.1 三类文件

| 类别 | 含义 | 文件 |
|---|---|---|
| **A. 完全落盘、工作区未动** | 已 commit 且 = HEAD | `lib/`：state · config · cdp · session · guard · md5fix · telemetry · bandit · log · concurrency；`bin/cmds/`：status · ready · close · sync · delete · md5fix · upload · monitor · stats · passrate；`targets/*` · `system.json` · 其余 docs |
| **B. 落盘过、后来又改（未落盘的增量叠在已提交基线上）** | 有 commit 基线 + 工作区 delta，delta 未提交 | `lib/upload.mjs` · `lib/submit.mjs` · `lib/sync.mjs` · `lib/delete.mjs` · `bin/cmds/cycle.mjs` · `bin/cmds/prep.mjs` · `bin/cmds/rounds.mjs` · `bin/weilai.mjs` · `docs/OPTIMIZATION-BACKLOG.md`（共 **9 个**） |
| **C. 从未落盘（未跟踪新文件）** | git 里不存在，纯工作区 | `docs/工作报告-2026-06-25-CLI实跑-冷启bug-jie6投放打通.md` · `docs/工作报告-2026-06-25-张晟钰双通道投放.md`（共 **2 个**） + 本报告 |

> 结论：**没有任何源码是「全新未跟踪」**——9 个改动文件都是在已提交基线上叠的增量；其中 `cycle.mjs` 是「落盘过(单趟骨架) → 工作区重写为(多轮收敛)」的典型「落盘过又改」。

### 1.2 git 客观证据

```
HEAD = 70fb006 (docs: 工作报告 2026-06-24)
git diff --stat（工作区 vs HEAD，全部未提交）：
  bin/cmds/cycle.mjs           | 62 +++   ← 单趟骨架 → 多轮收敛（重写）
  bin/cmds/prep.mjs            |  3       ← 穿 platform 快照给 delete
  bin/cmds/rounds.mjs          |  3       ← 穿 platform 快照给 delete
  bin/weilai.mjs               |  4       ← VALUE_FLAGS +rounds/round-wait；cycle help 文案
  docs/OPTIMIZATION-BACKLOG.md | 25       ← 追加 NO_CHOOSER bug 段 + ①②修复进度
  lib/delete.mjs               | 27       ← 可选 platform 入参 + 懒连接（dry 穿快照零 CDP）
  lib/submit.mjs               |  5       ← 空面板 total=0 → 1ms 短路返回
  lib/sync.mjs                 |  6       ← platform 非枚举属性挂 result（穿快照）
  lib/upload.mjs               | 12       ← kw 字段硬闸门 + fail-loud(注入0→E_GESTURE)
git diff --cached = 空（暂存区无内容）
node --check：上述 8 个 .mjs 全 OK（工作区语法自洽）
```

---

## 2. 逐文件落盘对账（已提交基线 ↔ 工作区增量）

> 每行：**已 commit 的部分**（落盘）｜**工作区新增、未 commit 的部分**（未落盘）。

| 文件 | 已落盘（commit） | 未落盘（工作区 delta） | 三源一致性 |
|---|---|---|---|
| `lib/upload.mjs` | 瞬态注入 `inject`/`injectBatch`、`runUpload` 编排、轮次 token 幂等、point7 max-mid 捕获、记 submissions（`d86ca2c`/`efa2043`/`df858fd`） | ① `kw` 字段硬闸门（拒非`魏文彬`）② **fail-loud**：注入 0 → 抛 `E_GESTURE`（line 154） | ✅ 报告§四声称 = diff 实物 |
| `lib/submit.mjs` | 逐文件超时（done>0+stall→提交已完成踢回未完成）、N 取确认弹窗（`d86ca2c`） | 空面板 `total==0` → 1ms 短路返回 `{click:'EMPTY'}`（line 26-29） | ✅ 一致 |
| `lib/sync.mjs` | reload 捕签名、Offset 翻页、批30/4并发拉审核、S1 增量、S1b 签名缓存、observe/recompute、结审 submissions（`d2aec32`/`db7a429`/`e4c3d91`/`df858fd`） | `Object.defineProperty(result,'platform',{enumerable:false})` —— 穿快照给 delete 且不泄漏进 `--json`（line 170-173） | ✅ 一致 |
| `lib/delete.mjs` | UI删1抓 set-opt 签名 + 重放、computeTargets(过审/被拒去重)、replayOk 校验（`f788d20`/`85d09a1`） | 可选 `platform` 入参 + 懒连接：dry-run 穿快照零 CDP；apply 时再连 + **再 guardEnter**（防穿快照与真删间漂移） | ✅ 一致 |
| `bin/cmds/cycle.mjs` | **单趟骨架**：ready→sync→delete→md5fix→\[upload\]（`d300eb4`，后接 upload） | **重写为多轮收敛**：`--rounds N`/`--round-wait MIN`、每轮删腾槽、穿快照、待上传清零提前收敛 | ✅（#11，落盘过又改的典型） |
| `bin/cmds/prep.mjs` | sync→delete(先dry后apply)→md5fix | delete 入参加 `platform: s.platform` | ✅ 一致 |
| `bin/cmds/rounds.mjs` | test-round/deliver-round（`efa2043`） | test-round 的 delete 入参加 `platform: s.platform` | ✅ 一致 |
| `bin/weilai.mjs` | argv 护栏、命令注册、退出码映射 | `VALUE_FLAGS` 加 `rounds`/`round-wait`；cycle help 改「多轮收敛」 | ✅ 一致 |
| `docs/OPTIMIZATION-BACKLOG.md` | A/C/S 系列台账、路线图、已结案疑点 | 追加「上传冷启首开竞速 + 静默空转 bug」整段 + ①②修复进度 + ⚠️未提交标注 | ✅ 一致 |

**交叉验证结论**：`工作报告-2026-06-25-CLI实跑` §四「代码改动（全部未提交）」表所列 8 项，**与 `git diff` 实物逐项吻合，无虚报、无遗漏、无『报告说改了但代码没改』或反之**。报告诚实度高。

---

## 3. 已落盘的功能版图（commit 维度）

按 git log 还原的主线（25 提交，最早 `5bd7b86` Phase0 → HEAD `70fb006`）：

| 阶段 | 提交 | 内容 | 验证态（文档声称） |
|---|---|---|---|
| Phase 0 骨架 | `5bd7b86` | 入口 + 配置层 + 只读 status | ✅ 97 件实测 |
| 计划① 基建 | `b4cc9cf`→`3b9ce41` | ready/guard/框架/sync/delete/md5fix/telemetry/cycle骨架/上传桩 | ✅ live（清盘后） |
| 混沌测试 | `ea718cd`→`b0c95d7` | 三并发 worker · 43 ready×95 干扰→0 误操作 · 修 P1/P2/P5 | ✅ |
| 真 bug 修 | `b249092` `3c2e533` | probeAccount 改 outerHTML；jie3.advId 1862→1849（冷会话收敛） | ✅ live |
| A 系列 10 bug | `85d09a1` | 台账解析守护/取值flag/guardEnter/CDP不吞错/delete重放校验/observe/norm连字符… | 离线12单测+live |
| C 系列提速 | `d2aec32` `4dad860` | C2/C3/C4（sync 19s→10.9s）；C1/C5/S8 ready 收敛（热路径 0.9s） | ✅ live |
| S1 增量同步 | `db7a429` `e4c3d91` | S1a mid 增量(查0.3s vs 3.4s)；S1b 签名缓存(TTL≥10min→7min·6s→1s) | ✅ live 对账13/13 |
| S5 bandit | `13ad129` | Beta-Bernoulli Thompson 采样模块 | 离线7/7 |
| 计划② 上传核心 | `d86ca2c` `efa2043` | inject 瞬态注入 + submit 逐文件超时 + bump + 轮次token + openView冷启重试 | ✅ **live 真创建素材** |
| Phase 4 pass-rate | `df858fd` | submissions.jsonl + point7 mid 捕获 + S5 接线 + passrate 命令 | 离线11/11 + mid read-only live |

**已落盘但仍是桩**：`submit.holdSubmit()` 抛 `E_NOT_IMPL`（计划②收尾项，等延迟挂起 TTL 探针）。

**仅在工作区（未落盘）的新能力**：
1. **上传 fail-loud**——无人值守命门修复（静默45min空转·退出0 → 秒级 E_GESTURE·退出15）。
2. **kw 字段硬闸门上 upload**——堵「绕台账用错字段误传」。
3. **cycle 多轮收敛（#11）**——每轮删腾槽，根治「只传不删塞满 jie3」。
4. **sync↔delete 共享平台快照（C档）**——同轮免重复 reload+翻页。

---

## 4. 台账数字链路核对（97 → 143，自洽）

| 时点 / 来源 | total | testing | sealed | delivering | delivered | scrapped |
|---|---|---|---|---|---|---|
| PLAN.md §二（原始基线） | 97 | 13 | 4 | 10 | 15 | 55 |
| 工作报告-06-24 §六 | 97 | 13 | 4 | 10 | 15 | 55 |
| 工作报告-06-25-CLI实跑 §五 | 143 | 49 | 0 | 24 | 15 | 55 |
| **本报告实跑 `status both`** | **143** | **49** | **0** | **24** | **15** | **55** |

**链路解释（06-24→06-25）**：张晟钰批 35 个新 Sub 视频入 `testing`（13→49，+36 与新批量级一致）；多轮迭代后过审件全部推 jie6 → `sealed` 清零(4→0)、`delivering` 10→24(+14 = 张晟钰+CLI实跑两批投放件)；`delivered`/`scrapped` 未变。**当前台账 = 06-25-CLI实跑 报告收尾态，逐字段一致，无漂移。**

jie3 待迭代细分（报告§五，待核实于下次 sync）：从没传 6 / 已被拒未到上限 13 / 审核中在飞 30 ／ 现在就能传 19（≈2 轮）。

---

## 5. Bug 台账（三源交叉）

| Bug | 来源 | 状态 | 落盘？ |
|---|---|---|---|
| A1–A9（台账解析/取值flag/guardEnter/CDP吞错/delete重放校验/observe/norm…） | buglog A 系列 | ✅ 全修，离线12+live | ✅ `85d09a1` |
| 混沌 P1/P2/P5（无限挂死/收敛重试/清残留标签） | 混沌报告 | ✅ 修；P6 回退 | ✅ `b0c95d7` |
| probeAccount innerText→outerHTML（账户名藏折叠器） | 06-23 | ✅ | ✅ `b249092` |
| jie3.advId 1862→1849（冷会话被弹回捷沅6） | 06-23 | ✅ | ✅ `3c2e533` |
| 「delete 将删 0」 | buglog 疑点 | ✅ 已结案=正确行为（平台交叉核实无 live 副本） | ✅ |
| **NO_CHOOSER 冷启首开竞速 + 静默空转** | 06-25-CLI §六 + buglog | 🟡 **失败模式已修（①②），根因(③④warm-up)未修** | ❌ **①②在工作区未落盘** |
| **worklist `test_reupload` 混「审核中」与「被拒」** | 06-25-CLI §八.1 | ⬜ 未修（⚠️继续迭代前必修，否则误传在飞件） | ❌ 未改 |
| delete 单目标行 E_SELECTOR（抽屉搜索结果1行抓不到） | 06-25-CLI §八.4 | ⬜ 未修（需翻页/重试兜底） | ❌ 未改 |
| jie6 mid 捕获 Runtime.evaluate 超时（非致命，下次 sync 回填） | 06-25-CLI §八.6 | ⬜ 容忍 | — |
| 06-25 张晟钰「张晟钰子串全删239」翻车 | 06-25-张晟钰 §二.2 | ✅ 经验已沉淀；CLI delete 路径从机制堵死（kw闸门+台账驱动+非全删） | 机制已落盘，闸门上 upload 未落盘 |

---

## 6. 文档与现实的出入点（需注意的过时/夸大）

1. **README.md / SKILL.md 严重过时**：仍称「Phase 0 骨架、upload 是桩、jie6 未通」。实况：上传 live 通过、jie6 投放经 CLI 打通。SKILL 自己注明「以仓库实况为准」，但首次接手者易被误导。**建议随提交一并更新。**
2. **HANDOFF.md 停在 `b0c95d7`（06-23）**，把「磁盘满假崩」「jie6 登录未通」列为头号阻塞——均已在 06-24/06-25 解除/绕过。**已被工作报告取代但未删/未更。**
3. **「逐文件超时被回退」的语义**：两份 06-25 报告提到 submit 逐文件超时「此前被回退」——指 **scratchpad 编排脚本**（iterate/deliver-jie6.mjs）里回退成等整批，**`lib/submit.mjs` 本体始终保留逐文件超时**（已读源码确认）。lib 是真源，scratchpad 不是。
4. **守卫「防45min空转」曾被夸大**：06-25-CLI §三.3 已自我纠正——①②修库**之前**守卫防不住（runUpload 内 inject→submit 顺序调用，submit 先空转45min 守卫才看到）。修了①②（未落盘）后才真生效。
5. **PLAN-1 模块状态表过时**（cycle=--skip-upload 骨架、upload=桩）——是计划①快照，已被计划②取代。

---

## 7. 当前阻塞与待办（按优先级）

**硬阻塞（卡死大面积推进）**
- **jie6 冷 profile 登录链**（#2/#3/#5 一箭三雕）：挡 jie6 ready/创意tab/delete/sweep/双实例。06-25 靠「手动登录留的 jie6 profile 仍有效」绕过，非根治。jie6=有钱号（投放中~49万），调试需谨慎。
- **磁盘**：H:(台账盘)~0.15G 偏低、C: 需为真上传(md5fix 单件~400MB)腾空间。

**优先待办**
1. ⚠️ **修 worklist 重传筛选**：`test_reupload` 改为只取 `last_status==2`（否则下一轮新件不足时误传 30 个审核中件）。**继续迭代前必做。**
2. **提交工作区 9 改动**（建议拆 3 commit，见 §8）。
3. **冷启 NO_CHOOSER 根因预防**（③④ warm-up：openUploadPanel 可交互探测 / ready 预热上传组件）。
4. **delete 单目标行 E_SELECTOR** 翻页/重试兜底。
5. 补投 jie6 第 10 个（卡 0% 那个）。

**架构投资（大批量才香）**
- **S2 飞轮**：单编排进程双 CDP（jie3 迭代 ∥ jie6 投放）+ 后台 md5fix 池 + **台账存盘锁**（防两进程 lost-update）。后台 md5fix 现在就能安全并行（只写 I:、不碰台账/Chrome）。
- hold-submit 转正（需延迟挂起 TTL 探针）；S6 ffprobe 预校验；S7 按 pass_rate 调重试预算；S9 API-direct（长期 R&D）。

---

## 8. 建议提交拆分（待用户批准）

工作区 9 改动建议拆 3 个干净 commit：
1. `fix: 上传冷启失败模式 fail-loud`（`upload.mjs` 注入0→E_GESTURE + `submit.mjs` 空面板1ms短路）—— 无人值守命门。
2. `feat: cycle 多轮收敛(#11) + upload 魏文彬 字段锁`（`cycle.mjs` 重写 + `weilai.mjs` flags + `upload.mjs` kw闸门）。
3. `perf: sync→delete 共享平台快照(最小解耦最大合并)`（`sync.mjs` 非枚举挂载 + `delete.mjs` 懒连接 + `prep/rounds.mjs` 穿快照）。
+ `docs:` 收 OPTIMIZATION-BACKLOG 更新 + 两份 06-25 报告 +（可选）刷新 README/SKILL/HANDOFF 过时点。

> 注：实跑用的 `round10.mjs`/`iterate-jie3.mjs`/`deliver-jie6.mjs`/`zsy-tracking.json` 等是 scratchpad 编排件（调用 lib 函数），**非 CLI 源码、不入 git**——与 `.gitignore` 约定一致。

---

## 9. 一页结论

- **代码主线**：计划①②全落地、双通道闭环 live 跑通、Phase1–4 接线——**均已落盘**。
- **未落盘**：06-25 第二段实跑的 9 处增量（fail-loud / kw锁 / cycle多轮 / 共享快照）+ NO_CHOOSER buglog + 2 份报告。**全部「落盘过又改」或「全新未跟踪」，无暂存、无半提交、`node --check` 全过。**
- **三源一致性**：工作报告声称的改动 = git diff 实物 = 源码现状，**逐项吻合，无虚报**；唯文档层（README/SKILL/HANDOFF/PLAN-1）有过时点待刷新。
- **最紧的一件事**：提交前先修 `test_reupload` 重传筛选（否则下轮误传审核中件）。
